#ifndef __TEXTURE_STORE_HPP__
#define __TEXTURE_STORE_HPP__

#include <filesystem>
#include <vector>

class TextureStore {
public: 
  TextureStore(const std::vector<std::filesystem::path> assets) {}

  // const sf::Texture& textureFor(
private:
};

#endif // !__TEXTURE_STORE_HPP__
